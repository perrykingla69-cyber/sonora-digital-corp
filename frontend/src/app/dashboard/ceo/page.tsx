import { CEODashboard } from '@/components/dashboards'

export const metadata = {
  title: 'HERMES CEO Control — Dashboard',
  description: 'Panel de control central para gestión de tenants y servicios',
}

export default function CEODashboardPage() {
  return <CEODashboard />
}
