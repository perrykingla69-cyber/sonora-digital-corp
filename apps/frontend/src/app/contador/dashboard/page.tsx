import ContadorDashboard from '@/components/contadores/ContadorDashboard';

export const metadata = {
  title: 'Dashboard Contador - HERMES',
  description: 'Vista principal de obligaciones y alertas fiscales'
};

export default function DashboardPage() {
  return <ContadorDashboard />;
}
