# apps/api

Entrada v2 del backend. Ya contiene bootstrap con `create_app()`, router agregador, schemas compartidos y routers modulares de sistema, auth, tenants, facturas, empleados, nómina preview, contactos, dashboard, leads, alertas y memoria/RAG básica.
