version: '3.9'

# docker-compose.vps.yml — Deploy en VPS Hostinger 187.124.85.191
# Estructura real del VPS: solo sonora-digital-corp/ y web/
# Ejecutar desde: /home/mystic/sonora-digital-corp/infra/
# Comando: docker compose -f docker-compose.vps.yml --env-file .env up -d

services:

  postgres:
    image: postgres:15-alpine
    container_name: hermes_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mystic_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-mystic_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - hermes_net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mystic_user}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: hermes_redis
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    networks:
      - hermes_net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: hermes_api
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-mystic_user}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-mystic_db}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      SECRET_KEY: ${SECRET_KEY}
      CORS_ORIGINS: ${CORS_ORIGINS:-https://sonoradigitalcorp.com}
      TELEGRAM_TOKEN: ${TELEGRAM_TOKEN}
      OLLAMA_URL: http://ollama:11434
      OLLAMA_MODEL: ${OLLAMA_MODEL:-qwen2.5:7b}
      OLLAMA_TIMEOUT: ${OLLAMA_TIMEOUT:-90}
      BRAIN_CACHE_TTL: ${BRAIN_CACHE_TTL:-3600}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      QDRANT_URL: http://qdrant:6333
      ENVIRONMENT: production
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - hermes_net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/status')"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://sonoradigitalcorp.com/api}
    container_name: hermes_frontend
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - hermes_net
    restart: unless-stopped

  # bot (Python legacy) — DESACTIVADO: conflicto con telegram-bot Node.js v2
  # bot:
  #   build:
  #     context: ../bot
  #     dockerfile: Dockerfile
  #   container_name: hermes_bot
  #   environment:
  #     TELEGRAM_TOKEN: ${TELEGRAM_TOKEN}
  #     API_URL: http://api:8000
  #   networks:
  #     - hermes_net
  #   restart: "no"

  ollama:
    image: ollama/ollama:latest
    container_name: hermes_ollama
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - hermes_net
    restart: unless-stopped
    environment:
      OLLAMA_HOST: 0.0.0.0:11434

  n8n:
    image: n8nio/n8n:latest
    container_name: hermes_n8n
    ports:
      - "127.0.0.1:5679:5678"
    environment:
      DB_TYPE: postgresdb
      DB_POSTGRESDB_DATABASE: ${POSTGRES_DB:-mystic_db}
      DB_POSTGRESDB_HOST: postgres
      DB_POSTGRESDB_PORT: 5432
      DB_POSTGRESDB_USER: ${POSTGRES_USER:-mystic_user}
      DB_POSTGRESDB_PASSWORD: ${POSTGRES_PASSWORD}
      N8N_BASIC_AUTH_ACTIVE: "true"
      N8N_BASIC_AUTH_USER: ${N8N_USER:-mystic}
      N8N_BASIC_AUTH_PASSWORD: ${N8N_PASSWORD}
      N8N_HOST: 0.0.0.0
      N8N_PORT: 5678
      N8N_PROTOCOL: https
      N8N_PATH: /n8n/
      WEBHOOK_URL: https://sonoradigitalcorp.com/n8n/
      N8N_SECURE_COOKIE: "true"
      N8N_EDITOR_BASE_URL: https://sonoradigitalcorp.com/n8n/
      DOCKER_API_URL: http://host.docker.internal:2375
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_TOKEN}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - n8n_data:/home/node/.n8n
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - hermes_net
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant:latest
    container_name: hermes_qdrant
    volumes:
      - qdrant_data:/qdrant/storage
    ports:
      - "127.0.0.1:6333:6333"
    networks:
      - hermes_net
    restart: unless-stopped
    environment:
      QDRANT__SERVICE__HTTP_PORT: 6333

  whatsapp:
    build:
      context: ../whatsapp
      dockerfile: Dockerfile
    container_name: hermes_wa
    ports:
      - "127.0.0.1:3001:3001"
    environment:
      PORT: 3001
      WA_API_KEY: ${WA_API_KEY}
      AUTH_DIR: /wa-data/auth
      WEBHOOK_URL: http://api:8000/api/wa/webhook
    volumes:
      - wa_data:/wa-data
    networks:
      - hermes_net
    restart: unless-stopped

  whatsapp-bridge:
    build:
      context: ../apps/whatsapp-bridge
      dockerfile: Dockerfile
    container_name: hermes_whatsapp_bridge
    ports:
      - "127.0.0.1:3002:3002"
    environment:
      PORT: 3002
      API_BASE: http://api:8000
      PUPPETEER_EXECUTABLE_PATH: /usr/bin/chromium-browser
    volumes:
      - whatsapp_sessions:/app/sessions
    depends_on:
      api:
        condition: service_healthy
    networks:
      - hermes_net
    restart: unless-stopped

  docker-proxy:
    image: alpine/socat
    container_name: hermes_docker_proxy
    command: TCP-LISTEN:2375,fork,reuseaddr UNIX-CONNECT:/var/run/docker.sock
    ports:
      - "127.0.0.1:2375:2375"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

  telegram-bot:
    build:
      context: ../apps/telegram-bot
      dockerfile: Dockerfile
    container_name: hermes_telegram_bot
    ports:
      - "127.0.0.1:3003:3003"
    environment:
      PORT: 3003
      API_BASE: http://api:8000
      DEFAULT_BOT_TOKEN: ${TELEGRAM_TOKEN}
      AUREA_BOT_TOKEN: ${AUREA_BOT_TOKEN:-}
      CLAUDE_BOT_TOKEN: ${CLAUDE_BOT_TOKEN:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      HERMES_ADMIN_CHAT_ID: ${HERMES_ADMIN_CHAT_ID:-}
      HERMES_ALERT_CHANNEL_ID: ${HERMES_ALERT_CHANNEL_ID:-}
    depends_on:
      api:
        condition: service_healthy
    networks:
      - hermes_net
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
  n8n_data:
  ollama_data:
  wa_data:
  qdrant_data:
  whatsapp_sessions:

networks:
  hermes_net:
    driver: bridge
