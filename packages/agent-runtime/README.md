# packages/agent-runtime

Paquete interno v2 para runtime de agentes.

Incluye un primer corte con:
- `SkillRegistry` para registrar capacidades/skills,
- `TenantPolicy` para decisiones básicas por tenant,
- `RuntimeManager` y `AgentRuntimeSession` para sesiones mínimas del runtime.
